version: "2"
authtoken: ${NGROK_AUTHTOKEN}   # 环境变量替换，但这里也可以直接写，或用环境变量
tunnels:
  backend:
    proto: http
    addr: backend:8000           # 指向 backend 容器服务名和端口
    inspect: false
  frontend:
    proto: http
    addr: frontend:80            # 指向前端容器服务名和端口
    inspect: false